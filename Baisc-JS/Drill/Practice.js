// const user = { name: "RV", age: 21 };
// user.name;        // dot notation — default choice
// user["age"];        // bracket notation

// const key = "name";
// console.log(user[key]);    // works — evaluates the variable
// // user[name];   // ReferenceError if no variable called `name` exists — bracket contents are evaluated as an expression, not treated as a literal string automatically. Confirmed hands-on.